import { OnInit } from '@angular/core';
import { GrowlItem, GrowlService } from './growl.service';
import { GrowlConfig } from './growl.config';
export interface MyGrowlItem extends GrowlItem {
    closing?: boolean;
}
export declare class GrowlComponent implements OnInit {
    private config;
    private growlService;
    constructor(config: GrowlConfig, growlService: GrowlService);
    growlItems: MyGrowlItem[];
    ngOnInit(): void;
    private _remove(msg, done?);
    close(index: number, done?: () => void): void;
    onAddMessage(msg: GrowlItem): void;
    onRemoveMessage(msg: MyGrowlItem): void;
}
